export interface PartnerContact {
  name: string;
  role: string;
  email: string;
  accentColor: string;
  badgeBg: string;
}

export interface PartnershipBenefit {
  title: string;
  description: string;
  dotColor: string;
}

export interface PartnershipStat {
  value: string;
  label: string;
  description: string;
  color: string;
}
